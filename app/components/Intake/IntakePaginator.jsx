import React from "react";
import { View, Text } from "react-native";

const IntakePaginator = ({ index }) => {
  return (
    <View>
      <Text>Paginator</Text>
    </View>
  );
};

export default IntakePaginator;
